load("@rules_d//d:defs.bzl", "d_library", "d_test")

d_library(
    name = "semver",
    srcs = glob(["source/*.d"]),
    imports = ["source"],
    visibility = ["//visibility:public"],
)

d_test(
    name = "semver_test",
    srcs = glob(["source/*.d"]),
)
